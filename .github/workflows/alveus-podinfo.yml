name: podinfo
"on":
  workflow_dispatch: {}
  push:
    paths:
    - .alveus/demo/manifests
    branches:
    - main
jobs:
  mock-prod:
    name: mock-prod
    needs:
    - mock-staging
    uses: ./.github/workflows/alveus-podinfo-mock-prod.yml
    secrets: inherit
  mock-staging:
    name: mock-staging
    uses: ./.github/workflows/alveus-podinfo-mock-staging.yml
    secrets: inherit
